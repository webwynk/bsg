# BSG Web Dashboard Global Agent Rules

- **Documentation Priority:** When working on the web dashboard or backend server, you MUST always follow the documentation and rules specified in the `bsg-web-dashboard` skill (`d:\Download\Game\.agents\skills\bsg_web_dashboard\SKILL.md` and its associated `docs/` folder).
- Never deviate from the Database Schema (RPC-only money movement, RLS enabled), the Security Protocols (Supabase Auth, SSR cookies), or the Design System (Tailwind tokens, responsive layout rules) without the user's explicit permission.
